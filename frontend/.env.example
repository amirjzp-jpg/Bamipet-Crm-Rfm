# Only needed for a SPLIT deployment (frontend and backend on different
# domains — e.g. Vercel + Render). Leave unset for Docker Compose / local
# dev, where the frontend and backend share an origin via nginx/Vite proxy.
#
# Set to your deployed backend's origin, no trailing slash:
VITE_API_BASE_URL=https://your-backend.onrender.com
